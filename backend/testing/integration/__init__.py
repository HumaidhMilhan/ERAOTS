"""
ERAOTS Integration Tests Package
=================================

Integration tests verify complete API workflows:
- Authentication flow (login, token validation)
- CRUD operations (employees, departments)
- Business processes (attendance, emergency)
- Real-time features (WebSocket events)

Prerequisites:
- Backend server running on localhost:8000
- Database seeded with test data (run test_database_seed.py)
"""
